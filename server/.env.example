# Database
# For local development with PostgreSQL (recommended):
DATABASE_URL="postgresql://mooja_user:mooja_password@localhost:5432/mooja_backend?schema=public&pool_timeout=10&connection_limit=10"
DIRECT_URL="postgresql://mooja_user:mooja_password@localhost:5432/mooja_backend?schema=public"
# For production (replace with your PostgreSQL connection string):
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public&sslmode=require&pool_timeout=10&connection_limit=20"
# DIRECT_URL="postgresql://user:password@host:5432/database?schema=public&sslmode=require"
# For local development with SQLite (fallback):
# DATABASE_URL="file:./dev.db"

# JWT (for org authentication)
JWT_SECRET=your_jwt_secret_here_change_in_production_minimum_32_chars
JWT_EXPIRATION=30d

# Security
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://yourdomain.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# App
PORT=3000
NODE_ENV=development
